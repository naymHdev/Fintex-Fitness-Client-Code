
const ForumCart = ({forum}) => {
    console.log(forum);

    const {author, content, likes, text, texts, timestamp} = forum || {};

    return (
        <div>
            
        </div>
    );
};

export default ForumCart;